# packages/sdk

Reserved. Versioned TypeScript client SDK generated from `apps/gateway`'s OpenAPI contract,
typed via `packages/shared-types`. Consumed by Web, Mobile, Widget, and external developers.
Scaffolded once `apps/gateway`'s contract exists (after M6).
