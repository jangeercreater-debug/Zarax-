# Server-only — never exposed to the browser bundle (no NEXT_PUBLIC_ prefix).
# Points at services/api directly for now; once apps/gateway exists, only this
# value changes — no dashboard code changes needed (see README).
BACKEND_URL=http://localhost:3000
RAG_SERVICE_URL=http://localhost:3005

NODE_ENV=development
