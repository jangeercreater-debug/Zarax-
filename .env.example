# Global variables shared across all apps/services.
# Each app/service also has its own .env.example for scoped variables —
# see apps/*/. env.example and services/*/.env.example.

NODE_ENV=development
LOG_LEVEL=info

# Set to a stable value per deployment; propagated into structured logs and traces.
DEPLOYMENT_REGION=local
